interface ICard {
    title: string
    price: Array
    icon: string
    unit: string
    totaltle: string
    total: number
}

interface IQuick {
    title: string
    icon: string
    iconColor: string
    route: string
    show: boolean
}